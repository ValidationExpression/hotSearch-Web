<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.haolan.hotsearchweb.mapper.UserMapper">

    <select id="selectUser" resultType="com.haolan.hotsearchweb.model.UserDO">
        select * from user order by id
    </select>

    <update id="update" parameterType="com.haolan.hotsearchweb.model.UserDO">
        update user set nickname = #{nickname}, sex = #{sex}, age = #{age}, user_pic = #{user_pic} where id = #{id}
    </update>
</mapper>
